# Copy this file to .env and fill in real values (never commit .env).

# Used by the browser app (safe to expose - anon key, not a secret).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Used only by scripts/import-rcdb.ts, run locally. NEVER expose to the browser/build.
SUPABASE_SERVICE_ROLE_KEY=
